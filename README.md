# 💬 شات لايف v2.0

## تشغيل المشروع

```bash
# 1. تثبيت المكتبات
npm install

# 2. تشغيل السيرفر
npm start

# 3. افتح المتصفح
http://localhost:3000
```

> للتطوير: `npm run dev` (يحتاج nodemon)

---

## بيانات المالك (افتراضية)
| | |
|---|---|
| **اليوزر** | `admin` |
| **كلمة المرور** | `admin1234` |

> ⚠️ غيّر كلمة المرور من لوحة التحكم ← إعدادات بعد أول تشغيل

---

## هيكل الملفات
```
livechat/
├── server.js          ← السيرفر + Socket.io + API
├── db.js              ← قاعدة البيانات SQLite
├── auth.js            ← مصادقة المالك
├── package.json
└── public/
    ├── index.html     ← واجهة الشات
    ├── login.html     ← صفحة الدخول
    ├── style.css      ← التصميم
    └── client.js      ← منطق الفرونت
```

---

## المستويات والصلاحيات
| المستوى   | الرمز | الصلاحيات |
|-----------|-------|-----------|
| المالك    | 👑    | كل شيء + لوحة التحكم |
| المشرف    | ⭐    | طرد / كتم / حذف رسائل |
| هوست      | 🎖️   | مميز فقط |
| VIP       | ✨    | مميز فقط |
| عضو       | —     | دردشة |
| زائر      | —     | دردشة |

---

## الإصلاحات في v2.0
- ✅ إصلاح `ownerTab` — تمرير `this` بدل استخدام `event` المهمل
- ✅ إصلاح `setAuthError` — كانت غير معرّفة في client.js
- ✅ فصل `ownerBan` و `ownerUnban` (حظر ورفع حظر منفصلان)
- ✅ إضافة تبويب **إعدادات** في لوحة التحكم (تغيير كلمة المرور)
- ✅ إضافة API: `/api/owner/mute`, `/api/owner/unmute`, `/api/owner/kick`, `/api/owner/change-password`
- ✅ مؤشر بصري على الأعضاء المكتومين (نقطة حمراء)
- ✅ زر رفع الكتم في مودال العضو (يتغير حسب الحالة)
- ✅ منع دخول نفس اليوزر من جلستين
- ✅ حماية: لا يمكن طرد أو كتم المالك
- ✅ تحقق من الخادم في صفحة الدخول (بدل إرسال مباشر)
- ✅ حد 200 رسالة في كل غرفة (RAM)
- ✅ إزالة ملف style.css الزائد من الجذر
- ✅ تحسين Responsive للشاشات الصغيرة


## نسخة KAWI Secure المعدلة

هذه النسخة تضيف:
- شكل كلاسيكي أقرب لصورة KAWI القديمة.
- ملف صلاحيات مركزي `permissions.js`.
- منع الزوار من الخاص والغرف الخاصة.
- منع أصحاب الرتب الأقل من التحكم في رتب أعلى أو مساوية.
- حماية إضافية: Security Headers، تحديد حجم JSON، Rate Limit للـ API والرسائل، وتنظيف أسماء المستخدمين والرسائل.
- دعم إعداد بيانات المالك من متغيرات البيئة `OWNER_USERNAME` و`OWNER_PASSWORD`.
